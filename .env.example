# Copy this to ".env". For the hackathon you don't need any keys — leave it empty.
# Add variables here only if a skill you build needs them. Never commit .env.

# Example (only if a script calls the Claude API directly; the VS Code extension
# handles chat auth separately and does NOT need this):
# ANTHROPIC_API_KEY=
